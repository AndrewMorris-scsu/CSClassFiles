int main () {
	
	for (cols in Huff array) {}
		for (rows in Huff array) {

			if (counter > threshold) {
				//If we will consider this to be a line.
				//Print out all of the pixels that voted for this line.

				for (int i = 0; i < pixels[i][j].size) {
					Dest.myfile.image_ptr[voter.x][voter.y] = 0;
				}

			}	
		}
	}	
}